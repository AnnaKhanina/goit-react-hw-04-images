import styled from '@emotion/styled';

const ImageGalleryItemStyled = styled.li`
  border-radius: ${p => p.theme.radii.sm};
  box-shadow: ${p => p.theme.boxShadow.item};
`;

export default ImageGalleryItemStyled;